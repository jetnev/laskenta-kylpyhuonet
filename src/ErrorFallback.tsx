import { Alert, AlertTitle, AlertDescription } from "./components/ui/alert";


export const ErrorFallback = ({
  error,
  resetErrorBoundary,
}: {
  error: Error;
  resetErrorBoundary: () => void;
      <
        <pre className="text-xs overflow-auto whitespace-pre-wrap mt-
        </pre>
          onClick={resetErrorBoundary}
          variant="outli
          Try again
      </AlertDescription>
        </pre>



          variant="outline"

          Try again

      </AlertDescription>
    </Alert>
  </div>
);
