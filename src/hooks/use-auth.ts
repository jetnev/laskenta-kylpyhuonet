import { useState, useEffect } from 'react';

interface User {
  avatarUrl: string;
  isOwner: boole
  id: string;
  const [loading, s

 

        setIsOwner(userInfo
        setUser(null);
  const [loading, setLoading] = useState(true);
      }

  useEffect(() => {
  return {
    loading
  };














    user,


  };

